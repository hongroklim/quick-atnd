/**
 *
 * @param {Int} cid - class id
 * @returns {pid | null}
 */
export const getLatestPid = (cid) => {
  return null;
}

/**
 * There is side effect.
 *
 * @param {Int} cid - class id
 * @returns {Int} pid
 */
export const generatePage = (cid) => {
  return 1;
}

/**
 *
 * @param {Int} pid - page id
 * @returns {Object}
 */
export const loadPage = (pid) => {
  return {pid: pid, label: '22.03.22 - 1'}
}
